# MOPS EPS 資料抓取測試結果

## 測試日期: 2025-07-02

## 測試目標
測試從公開資訊觀測站 (MOPS) 抓取 EPS（每股盈餘）資料的可行性

## 測試方法
1. 直接 HTTP GET 請求
2. 模擬表單 POST 提交
3. 使用不同的 URL 端點

## 測試結果

### ❌ 失敗：MOPS 程式化存取受限

**問題:**
- MOPS 網站有安全性機制，阻止程式化存取
- 回應錯誤訊息：「頁面無法執行 - 因為安全性考量」
- 需要瀏覽器環境和可能的 session 管理

**測試的 URL:**
- `https://mops.twse.com.tw/server-java/t164sb01`
- `https://mops.twse.com.tw/mops/web/t164sb01`
- `https://mops.twse.com.tw/mops/web/ajax_t164sb01`

## 結論與建議

### 🎯 建議使用 FinMind API
基於測試結果，建議在專案中使用 FinMind API 作為 EPS 資料的主要來源：

**優點:**
- ✅ 程式化 API 友善
- ✅ 資料格式標準化
- ✅ 有免費額度可測試
- ✅ 包含歷史資料
- ✅ 定期更新

**缺點:**
- ⚠️ 需要註冊 token（免費）
- ⚠️ 有 API 呼叫限制

### 🔄 MOPS 替代方案
如果未來需要從 MOPS 取得資料，可考慮：

1. **使用 Selenium/Puppeteer**
   - 模擬真實瀏覽器操作
   - 可處理 JavaScript 和 session

2. **使用第三方服務**
   - 如 TEJ、Wind 等付費資料服務
   - 已處理好資料格式和更新

3. **手動下載 + 自動解析**
   - 定期手動下載 XBRL 檔案
   - 程式解析本地檔案

## 實作建議

在現有專案中，建議：
1. 優先使用 FinMind `FinancialStatements` dataset
2. 實作 EPS QoQ（季度成長率）計算
3. 加入資料快取機制
4. 設定適當的錯誤處理

## 範例程式碼

```javascript
// 使用 FinMind 取得 EPS 資料
const epsData = await fetchFinMind('FinancialStatements', {
  data_id: '2330',
  start_date: '2023-01-01',
  end_date: '2024-12-31'
});

// 篩選 EPS 相關資料
const eps = epsData.filter(item =>
  item.type.includes('每股盈餘') ||
  item.type.includes('BasicEarningsPerShare')
);
```
